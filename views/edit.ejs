<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.0/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body class="bg-gray-100">
    <main class="max-w-md mx-auto mt-16 p-6 bg-white rounded-lg shadow-lg">
        <div class="flex justify-between items-center mb-6">
            <a href="/account"><button class="text-blue-600">Back</button></a>
            <a href="/logout" class="text-red-600">Logout</a>
        </div>

        <div class="flex flex-col items-center mb-6 ">
            <div class="w-24 h-24 overflow-hidden rounded-full mb-3 bg-gray-100">
                <img id="previewImage" class="rounded-full object-cover" src="/images/uploads/<%= user.profileimage %>" alt="">
            </div>
            <button id="editbtn" class="text-blue-600 capitalize">Edit Picture</button>
        </div>

        <form class="w-full" action="/update" method="post" enctype="multipart/form-data">
            <input hidden type="file" id="imageinput" name="image">
            <input class="w-full mb-3 px-4 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300" type="text" placeholder="Username" name="username" value="<%= user.username %>">
            <input class="w-full mb-3 px-4 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300" type="text" placeholder="Name" name="fullName" value="<%= user.fullName %>">
            <textarea class="w-full mb-3 px-4 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300 resize-none" type="text" name="bio" id="" cols="30" rows="3"><%= user.bio %></textarea>
            <button type="submit" class="w-full py-2 bg-blue-500 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring focus:border-blue-300">Update</button>
        </form>
    </main>

    <script>
        document.querySelector("#editbtn")
            .addEventListener("click", function () {
                document.querySelector("#imageinput").click();
            })
    </script>
</body>

</html>
